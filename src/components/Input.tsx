// import React from 'react';
// import styled from 'styled-components';

const TextInput = () => {
  return <input type="text" placeholder="텍스트를 입력해주세요." />;
};

const NumberInput = () => {
  return <input type="" placeholder="숫자를 입력해주세요." />;
};

export { TextInput, NumberInput };
